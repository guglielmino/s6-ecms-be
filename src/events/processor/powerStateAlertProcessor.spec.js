import chai from 'chai';
import sinon from 'sinon';
import PowerStateAlertProcessor from './powerStateAlertProcessor';
import mockery from "mockery";

chai.should();
const expect = chai.expect;

mockery.enable({
  warnOnReplace: false,
  warnOnUnregistered: false,
});

describe('PowerStateAlertProcessor', () => {

});
